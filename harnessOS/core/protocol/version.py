"""Version constants used by protocol and template compatibility checks."""

HARNESSOS_VERSION = "3.5.0"
SUPPORTED_PACK_MANIFEST_SCHEMA_VERSIONS = frozenset({"1"})
SUPPORTED_CONNECTOR_DESCRIPTOR_SCHEMA_VERSIONS = frozenset({"1"})

