# V3.5 Integration Docs

This directory contains V3.5 integration guidance and validation baselines.

Current status:

- Phase0 baseline is recorded in `v3_5_phase0_baseline.md`.
- Python SDK contract is recorded in `sdk_contract.md`.
- Minimal BFF smoke guidance is recorded in `bff_minimal_smoke.md`.
- TypeScript SDK core client and React hooks are implemented under `sdk/typescript`.

V3.5-MVP is complete. V3.5-E1 TypeScript SDK Core Client is complete. V3.5-E2 React Hooks are ready for dev/local UI integration smoke. Full external app readiness still requires the Full BFF Template, Pack/Connector Template, Embed Contract, and platform-neutral Reference App.
