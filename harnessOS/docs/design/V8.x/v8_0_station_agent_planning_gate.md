# V8-0 Station Agent Planning Gate

文档状态：V8-0 planning gate。

目标：

```text
冻结 V8 station-agent workflow pilot 的 PRD、目标架构、gap、验收门槛和 No False Green。
```

交付：

```text
V8 target PRD
V8 target architecture
V8 current gap analysis
V8 drawio gap diagram
V8 development and acceptance plan
V8 milestone roadmap
V8 acceptance gate matrix
V8 claim guard
V8 external audit entrypoint
```

验收：

```text
V8 文档索引完整。
drawio XML valid。
V7 baseline 没有被升级为 Agent executor ready。
V8-1 不得在 V8-0 external audit 接受前实现。
```

