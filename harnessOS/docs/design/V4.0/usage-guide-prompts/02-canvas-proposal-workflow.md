Create a technical usage guide infographic for harnessOS V4.0 canvas proposal workflow.

Scene: a workflow canvas with a user dragging a node from "节点库" onto the canvas. The dropped node appears as a translucent ghost node marked "Pending Proposal". A side panel shows "PatchDiffDTO". A separate confirmation panel shows "用户确认后应用". Use arrows to show the correct flow: Node/Edge/Inspector Intent -> BFF Proposal -> Patch Queue -> Editing Panel Confirmation -> BFF Truth Refresh.

Visual style: clean documentation infographic, light UI, precise panels and arrows, readable Simplified Chinese UI labels, professional product guide tone.

Key Chinese labels: "画布不是 runtime truth", "只生成 proposal", "Apply 前 draft revision 不变", "Apply 后从 BFF truth 刷新".

Safety and accuracy constraints: do not depict direct canvas-to-runtime mutation, direct WorkflowDraft write, direct WorkflowStore write, /v1/rpc browser calls, /v1/events/subscribe, auto apply, auto publish, or agent execution.
