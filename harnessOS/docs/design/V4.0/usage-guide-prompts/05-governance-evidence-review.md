Create a governance evidence review guide image for harnessOS V4.0.

Scene: a Governance Review panel shows an evidence chain timeline with proposal_id, handoff_id, user_confirmed, policy_decision, correlation_id, redaction_status, and created_at. The panel is read-only and has navigation controls but no execute buttons. A small canvas preview sits in the background.

Visual style: clean audit dashboard illustration, professional, light theme, structured table and timeline, no dark dramatic cyber style, no unnecessary decoration.

Key Chinese labels: "Evidence Review 只读", "operation evidence", "redaction passed", "EventBridge 只触发 refresh".

Safety and accuracy constraints: no Apply, Publish, Approve, Reject, Execute, Run, auto action, secret values, tokens, Authorization headers, Bearer strings, raw traces, raw artifacts, raw connectors, raw prompts, or signed URLs.
