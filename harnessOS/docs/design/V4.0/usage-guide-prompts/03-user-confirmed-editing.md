Create a workflow operation guide image for harnessOS V4.0 governed editing.

Scene: an Editing Panel is open inside Workflow Console. It shows a patch summary, risk flags, a selected patch queue item, and two clear user-controlled buttons: "确认应用" and "拒绝". A high-risk patch row is disabled with a visible warning "requires_approval=true cannot direct apply". In the background, the workflow canvas remains unchanged until confirmation.

Visual style: precise SaaS UI instructional illustration, light theme, dense but organized, no marketing hero layout, crisp typography, subtle shadows, 16:10 aspect ratio.

Key Chinese labels: "用户显式确认", "source=editing_panel", "high-risk blocked", "apply/reject/publish 后重新拉取 BFF truth".

Safety and accuracy constraints: never show "自动应用", "自动发布", "Agent 已执行", "controlled executor ready", or "production ready". Do not include secrets, tokens, raw payloads, or raw artifact content.
