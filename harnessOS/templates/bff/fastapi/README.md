# Full FastAPI BFF Template Scaffold

This directory is reserved for V3.5-F Full BFF Template.

Phase0 must not implement a BFF. The later template will include scope binding, capability token validation, RPC proxy, EventSource proxy, CORS, and denylist behavior.
