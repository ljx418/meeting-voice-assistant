# BFF Template Scaffold

This directory is reserved for V3.5 BFF examples and templates.

`fastapi_minimal` belongs to V3.5-D2 Minimal BFF Smoke. `fastapi` belongs to V3.5-F Full BFF Template. `node` is optional.
