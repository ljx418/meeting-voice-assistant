# 长时工程任务

## 目标

为 HarnessOS 的 Headless-first 工作流平台设计并验证一个可审计的工程任务执行流程。

## 约束

- 不允许 Agent 自动修改代码。
- 不允许自动发布。
- 所有持久操作都必须用户确认。
- 每个阶段必须产出 artifact。
- 失败阶段必须可重跑并保留 attempt history。

## 期望流程

产品规划、规格梳理、项目计划蓝图、总架构评审、子阶段计划审计、子阶段架构评审、开发实施、开发验收、代码检视、E2E 验收、人工确认。

