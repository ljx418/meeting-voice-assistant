# AgentOS 架构

AgentOS 采用 proposal-first 的工作流控制台架构。画布用于展示草案、运行状态和治理证据，真实修改必须经过 BFF 的受控路径。

## 核心主题

- BFF 负责权限、治理和 DTO redaction。
- Runtime 负责执行工作流节点。
- Agent 助手只能生成建议和交接，不能自动执行 mutation。

## 关键知识点

所有持久修改都需要用户确认。EventBridge 只触发 refresh，前端不能从事件 payload 构造 truth。
